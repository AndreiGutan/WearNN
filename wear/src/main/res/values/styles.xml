<resources>

    <style name="MainActivityTheme.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/darker_gray</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_nn_splash</item>
        <item name="postSplashScreenTheme">@android:style/Theme.DeviceDefault</item>
    </style>

    <!-- Define a theme for Wear OS that extends from AppCompat -->
    <style name="Theme.WearAppCompat" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customizations for AppCompat theme on Wear OS -->
        <!-- Wear OS specific color scheme adjustments could go here -->
    </style>
</resources>